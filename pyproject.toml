[project]
name = "terminal-agent"
version = "0.1.0"
description = "A coding agent inside your terminal"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.5.0",
    "dotenv>=0.9.9",
    "openai>=3.6.0",
    "rich>=15.0.0",
    "tiktoken>=0.14.0",
]
